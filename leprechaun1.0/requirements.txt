Flask==2.1.3
Flask-BasicAuth==0.2.0
gunicorn==20.1.0  # For Heroku deployment
requests==2.28.1  # To fetch data from APIs (Polygon.io, etc.)
ibapi==9.81.1.post1   # Interactive Brokers API
selenium==4.5.0   # For automating browser tasks (e.g., key generation)
numpy==1.24.0     # For calculations (volatility, moving averages, etc.)unittest2==1.1.0

